import java.awt.*;
import java.util.*;
import javax.swing.*;

public class map_handler
{
    Image bg_image;
    
    public map_handler(String bg, content_handler con)
    {
        bg_image = bg_image = Toolkit.getDefaultToolkit().getImage(bg);
    }
}
